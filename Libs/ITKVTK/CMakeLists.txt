FILE(GLOB ITKVTK_srcs ./*.h ./*.hxx)
if(TARGET ITKVTK)
    message("ITKVTK already exists")
else(TARGET ITKVTK)
    ADD_LIBRARY (ITKVTK STATIC ${ITKVTK_srcs})
    set_target_properties(ITKVTK PROPERTIES LINKER_LANGUAGE CXX)
endif(TARGET ITKVTK)
