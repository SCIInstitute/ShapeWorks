import setuptools

setuptools.setup(
   name='DeepSSMUtils',
   version='1.0',
   description='Python functions for DeepSSM',
   packages=setuptools.find_packages(),
   install_requires=['requests'], #external packages as dependencies
)