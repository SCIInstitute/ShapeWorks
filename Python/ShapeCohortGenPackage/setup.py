import setuptools

setuptools.setup(
   name='ShapeCohortGen',
   version='1.0',
   description='Python functions for generating cohorts of parameterized shapes.',
   packages=setuptools.find_packages(),
   install_requires=['requests'], #external packages as dependencies
)
