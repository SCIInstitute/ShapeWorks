import setuptools

setuptools.setup(
   name='DataAugmentationUtils',
   version='1.0',
   description='Python functions for running data augmentation.',
   packages=setuptools.find_packages(),
   install_requires=['requests'], #external packages as dependencies
)